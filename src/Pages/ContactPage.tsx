import React from 'react';

import {Data} from '../../global';

const ContactPage: React.FC<Data> = (props: Data) => {

    return (
        <p>ContactPage Works!</p>
    );
}

export default ContactPage;